<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="App1.AiChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="30,20">
        <Grid.RowDefinitions>
     <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
         <RowDefinition Height="Auto" />
       <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
     </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
          Text="AI Chat Demo"
Style="{StaticResource TitleTextBlockStyle}"
         HorizontalAlignment="Center"
        Margin="0,0,0,20" />

        <!-- Prompt Input -->
        <TextBox Grid.Row="1"
                 x:Name="PromptEntry"
    PlaceholderText="Enter your prompt here..."
         Text="Provide the molecular formula for glucose."
        FontSize="16"
         Margin="0,0,0,20" />

        <!-- Ask AI Button -->
        <Button Grid.Row="2"
     x:Name="CounterBtn"
                Content="Ask AI"
                Click="OnCounterClicked"
            HorizontalAlignment="Stretch"
                Margin="0,0,0,20" />

        <!-- Response Label -->
 <TextBlock Grid.Row="3"
              Text="Response:"
          FontWeight="Bold"
           Margin="0,0,0,10" />

        <!-- Response Area -->
        <Border Grid.Row="4"
    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
    BorderThickness="1"
   Background="{ThemeResource LayerFillColorDefaultBrush}"
       Padding="10"
           CornerRadius="5">
            <TextBox x:Name="ResponseEditor"
       PlaceholderText="AI response will appear here..."
     IsReadOnly="True"
   FontSize="14"
           Background="Transparent"
BorderThickness="0"
       TextWrapping="Wrap"
     AcceptsReturn="True"
      ScrollViewer.VerticalScrollBarVisibility="Auto" />
        </Border>
    </Grid>
</Page>
